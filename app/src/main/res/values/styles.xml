<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ProgressBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorLeft</item>
        <item name="colorPrimaryDark">@color/colorLeft</item>
        <item name="colorAccent">@color/colorLeft</item>
    </style>
    <style name="RadioButtonTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorLeft</item>
        <item name="colorPrimaryDark">@color/colorLeft</item>
        <item name="colorAccent">@color/colorLeft</item>
    </style>
</resources>
